import styled from "styled-components"

export const Row = styled.div`
    padding: 0;
    margin: 0;
    box-sizing: border-box;
    display: flex;
    flex-flow: row;
    flex-wrap: nowrap;
    overflow: hidden;
    width: 100%;
`